#@ load("@ytt:data", "data") 
---
apiVersion: carto.run/v1alpha1
kind: Workload
metadata:
  name: #@ data.values.artifactId
  labels:
    apps.tanzu.vmware.com/workload-type: web
spec:
  source:
    git:
      url: #@ "https://github.com/dektlong/" + data.values.gitRepo
      ref:
        branch: main
